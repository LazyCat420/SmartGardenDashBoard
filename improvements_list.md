1. Get the grid/chart/table view working in the plants tab. Change it to Grid/List for the views instead of chart/table. 

2. A leaderboard tab to be able to track progess of the plants. Allow the user to be able to select which plants to include in the leaderboard/charts. We can also make categories based on the plant type and show the leaderboard for each category. That way we are comparing simliar plants and the growth rates rather than comparing different plant types that grow at different rates anyhow so the data isn't useful. We need to fix the leaderboard currently its unable to fetch the data. 

3. Allow the user to set the model to use for the LLM. In this case we are using LMStudio. There should be a dropdown with the models avaliable for selection we should be able to fetch it via api look at LMStudio documentation online for help. User should also be allowed to set the endpoint if they have a different address for the api. While doing this we also have to figure out why it was failing to run the data on LMStudio even though it was able to connect to the api. 





