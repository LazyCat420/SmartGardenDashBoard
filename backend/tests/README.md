# Backend Tests

This directory contains automated tests for the backend API and services.

## Files

- `test_api.py`: General API endpoint tests (Plants, Leaderboard, LLM).
- `test_tasks.py`: Specific tests for Task management (CRUD, Toggle Status).

## Running Tests

Run tests from the project root:

```bash
pytest backend/tests
```
