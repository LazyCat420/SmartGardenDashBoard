1. A leaderboard tab to be able to track progess of the plants. Allow the user to be able to select which plants to include in the leaderboard/charts. We can also make categories based on the plant type and show the leaderboard for each category. That way we are comparing simliar plants and the growth rates rather than comparing different plant types that grow at different rates anyhow so the data isn't useful. Currently it is faling to fetch we need to fix it. 

2. Allow the user to set the model to use for the LLM. Right now the API Endpoint is fine but we should add a dropdown menu that has a list of the models that can be fetched via api look up LMStudio documentation online for help on how to do it. The api endpoint is http://10.0.0.231:1234/v1/chat/completions 



