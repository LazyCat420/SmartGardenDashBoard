{
  "name": "smartgarden-dashboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "bash deploy.sh",
    "deploy:dry": "bash deploy.sh --dry-run"
  },
  "dependencies": {
    "jsdom": "^27.2.0"
  }
}
