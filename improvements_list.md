1. Make the view section of the My Plants dashboard better where it shows the actual data in a spreadsheet like view along with the charts and graphs. We need spreadsheets/charts and graphs for all the data we collect from the plants. 

2. For the watering section we need to display the feeding data of what fertilizers/recipes were used and when. 

3. Add search functionality to the My Plants dashboard. 

4. Add a zipcode seach feature to the weather dashboard. 

5. A leaderboard tab to be able to track progess of the plants. Allow the user to be able to select which plants to include in the leaderboard/charts. We can also make categories based on the plant type and show the leaderboard for each category. That way we are comparing simliar plants and the growth rates rather than comparing different plant types that grow at different rates anyhow so the data isn't useful. 

6. Allow the user to set the model to use for the LLM. In this case we are using LMStudio. User should also be allowed to set the endpoint if they have a different address for the api. While doing this we also have to figure out why it was failing to run the data on LMStudio even though it was able to connect to the api. 

7.



